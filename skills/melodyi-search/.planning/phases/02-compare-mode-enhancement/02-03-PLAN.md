---
phase: 02-compare-mode-enhancement
plan: 03
type: execute
wave: 3
depends_on: [02-01, 02-02]
files_modified:
  - melodyi_search/domain/models/search_result.py
  - tests/domain/models/test_search_result.py
  - tests/domain/services/test_execution_strategy.py
autonomous: true
requirements: [COMP-06]
user_setup: []

must_haves:
  truths:
    - "UnifiedSearchResult 包含 session_id 字段"
    - "session_id 可通过 execute_comparison() 返回值获取"
    - "测试验证 session_id 正确传递"
  artifacts:
    - path: "melodyi_search/domain/models/search_result.py"
      provides: "统一搜索结果模型"
      contains: "session_id: Optional[str]"
    - path: "tests/domain/models/test_search_result.py"
      provides: "模型测试"
      contains: "test_session_id_field"
  key_links:
    - from: "execution_strategy.py:execute_comparison()"
      to: "UnifiedSearchResult.session_id"
      via: "unified_result.session_id = session_id"
      pattern: "session_id"
---

<objective>
扩展 UnifiedSearchResult 添加 session_id 字段，完成 Phase 2 测试覆盖

Purpose: 使 Compare 模式返回的结果包含 session_id，便于历史追溯
Output: search_result.py 修改 + 测试覆盖
</objective>

<execution_context>
@C:/workspace/melodyi-skills/skills/melodyi-search/.claude/get-shit-done/workflows/execute-plan.md
@C:/workspace/melodyi-skills/skills/melodyi-search/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-compare-mode-enhancement/02-CONTEXT.md
@.planning/phases/02-compare-mode-enhancement/02-RESEARCH.md
@.planning/phases/02-compare-mode-enhancement/02-01-PLAN.md
@.planning/phases/02-compare-mode-enhancement/02-02-PLAN.md

<interfaces>
<!-- Executor 需要的接口定义 -->

From melodyi_search/domain/models/search_result.py (现有代码):
```python
class UnifiedSearchResult(BaseModel):
    """统一搜索结果，暴露给 Agent/CLI"""

    provider: str = Field(..., description="响应的提供商")
    response_time_ms: int = Field(..., ge=0, description="响应时间(毫秒)")
    results: List[SearchResultItem] = Field(default_factory=list, description="搜索结果列表")
    comparison_log: Optional[dict] = Field(default=None, description="比对模式内部数据")
    error: Optional[SearchError] = Field(default=None, description="错误及指导")

    def is_success(self) -> bool: ...
    def has_results(self) -> bool: ...
```

From execution_strategy.py (Plan 02-02 修改):
```python
unified_result.session_id = session_id  # 添加 session_id
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 添加 session_id 字段到 UnifiedSearchResult</name>
  <files>melodyi_search/domain/models/search_result.py</files>
  <read_first>
    - melodyi_search/domain/models/search_result.py
    - melodyi_search/domain/services/execution_strategy.py
  </read_first>
  <behavior>
    - Test 1: UnifiedSearchResult 包含 session_id 字段
    - Test 2: session_id 默认值为 None
    - Test 3: session_id 可被赋值
  </behavior>
  <action>
    修改 melodyi_search/domain/models/search_result.py 的 UnifiedSearchResult 类：

    1. **添加 session_id 字段** (COMP-06 决策):
       ```python
       class UnifiedSearchResult(BaseModel):
           """统一搜索结果，暴露给 Agent/CLI"""

           provider: str = Field(..., description="响应的提供商")
           response_time_ms: int = Field(..., ge=0, description="响应时间(毫秒)")
           results: List[SearchResultItem] = Field(default_factory=list, description="搜索结果列表")
           comparison_log: Optional[dict] = Field(default=None, description="比对模式内部数据")
           error: Optional[SearchError] = Field(default=None, description="错误及指导")
           session_id: Optional[str] = Field(default=None, description="对比会话 ID (COMP-06)")  # 新增字段
       ```

    2. **字段位置**: 添加在 error 字段之后，保持逻辑分组

    3. **类型定义**: Optional[str]，默认值为 None，正常模式下无 session_id

    4. **文档字符串**: description 描述为 "对比会话 ID (COMP-06)"

    **关键约束**:
    - session_id 为可选字段（正常模式不返回）
    - Compare 模式下由 execution_strategy.py 赋值
    - 使用 Pydantic Field 定义，保持类型安全
  </action>
  <verify>
    <automated>grep -c "session_id: Optional\[str\]" melodyi_search/domain/models/search_result.py | grep -q "^1$"</automated>
  </verify>
  <done>
    - session_id 字段存在于 UnifiedSearchResult 类
    - session_id 类型为 Optional[str]
    - session_id 默认值为 None
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 添加 session_id 字段测试</name>
  <files>tests/domain/models/test_search_result.py</files>
  <read_first>
    - tests/domain/models/test_search_result.py
    - melodyi_search/domain/models/search_result.py
  </read_first>
  <behavior>
    - Test 1: UnifiedSearchResult 默认 session_id 为 None
    - Test 2: session_id 可被赋值
    - Test 3: session_id 可序列化为 JSON
  </behavior>
  <action>
    修改 tests/domain/models/test_search_result.py，添加 session_id 测试：

    1. **添加测试方法**:
       ```python
       def test_unified_search_result_session_id_default():
           """测试 session_id 默认值为 None"""
           result = UnifiedSearchResult(
               provider="test",
               response_time_ms=100,
               results=[]
           )
           assert result.session_id is None

       def test_unified_search_result_session_id_can_be_set():
           """测试 session_id 可被赋值"""
           result = UnifiedSearchResult(
               provider="test",
               response_time_ms=100,
               results=[],
               session_id="20260509-143052-a1b2"
           )
           assert result.session_id == "20260509-143052-a1b2"

       def test_unified_search_result_session_id_json_serialization():
           """测试 session_id 可序列化为 JSON"""
           result = UnifiedSearchResult(
               provider="test",
               response_time_ms=100,
               results=[],
               session_id="20260509-143052-a1b2"
           )
           json_str = result.model_dump_json()
           assert "session_id" in json_str
           assert "20260509-143052-a1b2" in json_str
       ```

    2. **导入依赖**:
       确保 `from melodyi_search.domain.models.search_result import UnifiedSearchResult` 存在

    文件位置: tests/domain/models/test_search_result.py
    新增测试数: 3 个
  </action>
  <verify>
    <automated>pytest tests/domain/models/test_search_result.py -v --tb=short</automated>
  </verify>
  <done>
    - test_unified_search_result_session_id_default 通过
    - test_unified_search_result_session_id_can_be_set 通过
    - test_unified_search_result_session_id_json_serialization 通过
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 完善集成测试验证 session_id 返回</name>
  <files>tests/domain/services/test_execution_strategy.py</files>
  <read_first>
    - tests/domain/services/test_execution_strategy.py
    - melodyi_search/domain/services/execution_strategy.py
    - melodyi_search/domain/models/search_result.py
  </read_first>
  <behavior>
    - Test 1: execute_comparison() 返回 UnifiedSearchResult 包含 session_id
    - Test 2: session_id 格式为 YYYYMMDD-HHMMSS-XXXX
    - Test 3: 正常模式 (execute_normal) 不返回 session_id
  </behavior>
  <action>
    在 tests/domain/services/test_execution_strategy.py 的 TestComparisonPersistence 类中添加测试：

    1. **添加 test_result_contains_session_id**:
       ```python
       def test_result_contains_session_id(self, strategy, recorder, temp_db, mock_provider):
           """测试 execute_comparison() 返回 session_id (COMP-06)"""
           request = ProviderSearchRequest(query="test", max_results=10)
           result = strategy.execute_comparison(
               providers=[mock_provider],
               request=request,
               recorder=recorder
           )

           # 验证 session_id 存在
           assert result.session_id is not None

           # 验证格式 YYYYMMDD-HHMMSS-XXXX
           session_id = result.session_id
           assert session_id.count('-') == 2
           parts = session_id.split('-')
           assert len(parts[0]) == 8  # YYYYMMDD
           assert len(parts[1]) == 6  # HHMMSS
           assert len(parts[2]) == 4  # XXXX

       def test_normal_mode_no_session_id(self, strategy, mock_provider):
           """测试正常模式不返回 session_id"""
           request = ProviderSearchRequest(query="test", max_results=10)
           result = strategy.execute_normal(
               providers=[mock_provider],
               request=request
           )

           # 正常模式无 session_id
           assert result.session_id is None
       ```

    2. **覆盖 COMP-06 需求**:
       - test_result_contains_session_id (COMP-06)
       - test_normal_mode_no_session_id (验证正常模式不影响)

    文件位置: tests/domain/services/test_execution_strategy.py
    新增测试数: 2 个
  </action>
  <verify>
    <automated>pytest tests/domain/services/test_execution_strategy.py::TestComparisonPersistence -v --tb=short</automated>
  </verify>
  <done>
    - test_result_contains_session_id 通过
    - test_normal_mode_no_session_id 通过
    - session_id 格式验证通过
  </done>
</task>

<task type="auto">
  <name>Task 4: 运行完整测试套件验证 Phase 2</name>
  <files>tests/domain/services/test_comparison_recorder.py, tests/domain/services/test_execution_strategy.py, tests/domain/models/test_search_result.py</files>
  <read_first>
    - tests/domain/services/test_comparison_recorder.py
    - tests/domain/services/test_execution_strategy.py
    - tests/domain/models/test_search_result.py
  </read_first>
  <action>
    运行完整测试套件验证 Phase 2 所有需求:

    1. **运行测试命令**:
       ```bash
       pytest tests/domain/services/ tests/domain/models/test_search_result.py -v --tb=short
       ```

    2. **验证需求覆盖**:
       - COMP-01: test_write_search_results_with_rank 通过
       - COMP-02: test_execute_comparison_persists_session 通过
       - COMP-03: test_write_session_success 通过
       - COMP-04: test_write_search_results_with_rank (rank 验证) 通过
       - COMP-05: test_write_provider_result_success 通过
       - COMP-06: test_result_contains_session_id 通过
       - COMP-07: test_daemon_thread_waits_for_completion 通过

    3. **预期结果**:
       - 所有测试通过
       - 测试覆盖率 > 80%
       - 无遗留失败测试

    如果测试失败，根据错误信息修复代码。
  </action>
  <verify>
    <automated>pytest tests/domain/services/ tests/domain/models/test_search_result.py -v --tb=short 2>&1 | grep -c "passed"</automated>
  </verify>
  <done>
    - 所有测试通过
    - COMP-01~07 需求全部验证
    - Phase 2 测试覆盖完成
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Domain Model → External | session_id 为本地生成，无外部暴露 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Information Disclosure | session_id in JSON output | accept | session_id 为本地标识符，不含敏感信息 |
</threat_model>

<verification>
## Per-Task Verification

| Task | Automated Command | Status |
|------|-------------------|--------|
| Task 1 | `grep -c "session_id: Optional\[str\]" melodyi_search/domain/models/search_result.py` | ⬜ pending |
| Task 2 | `pytest tests/domain/models/test_search_result.py -v` | ⬜ pending |
| Task 3 | `pytest tests/domain/services/test_execution_strategy.py::TestComparisonPersistence -v` | ⬜ pending |
| Task 4 | `pytest tests/domain/services/ tests/domain/models/test_search_result.py -v` | ⬜ pending |

## Phase Completion

Phase 2 完成后运行:
```bash
pytest tests/ -v --tb=short
```
</verification>

<success_criteria>
- UnifiedSearchResult 包含 session_id 字段
- session_id 默认值为 None
- session_id 可被赋值并序列化
- execute_comparison() 返回包含 session_id 的结果
- execute_normal() 不返回 session_id
- 所有测试通过
</success_criteria>

<output>
完成后创建 `.planning/phases/02-compare-mode-enhancement/02-03-SUMMARY.md`
</output>
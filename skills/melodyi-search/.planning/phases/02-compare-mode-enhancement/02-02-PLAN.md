---
phase: 02-compare-mode-enhancement
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - melodyi_search/domain/services/execution_strategy.py
  - tests/domain/services/test_execution_strategy.py
autonomous: true
requirements: [COMP-02, COMP-07]
user_setup: []

must_haves:
  truths:
    - "execute_comparison() 调用 ComparisonRecorder"
    - "后台线程设置 daemon=False"
    - "主线程等待后台线程完成 (timeout=10)"
    - "超时后记录 WARNING 日志继续退出"
  artifacts:
    - path: "melodyi_search/domain/services/execution_strategy.py"
      provides: "执行策略修改"
      contains: "daemon=False"
      contains: "thread.join(timeout=10)"
    - path: "tests/domain/services/test_execution_strategy.py"
      provides: "集成测试"
      contains: "TestComparisonPersistence"
  key_links:
    - from: "melodyi_search/domain/services/execution_strategy.py"
      to: "melodyi_search/domain/services/comparison_recorder.py"
      via: "ComparisonRecorder"
      pattern: "recorder\\."
    - from: "execution_strategy.py:execute_comparison()"
      to: "threading.Thread"
      via: "daemon=False"
      pattern: "daemon=False"
---

<objective>
修改 ExecutionStrategy 集成 ComparisonRecorder，修复 daemon thread 问题

Purpose: 确保 Compare 模式执行后数据持久化完成，修复后台线程数据丢失问题
Output: execution_strategy.py 修改 + 集成测试
</objective>

<execution_context>
@C:/workspace/melodyi-skills/skills/melodyi-search/.claude/get-shit-done/workflows/execute-plan.md
@C:/workspace/melodyi-skills/skills/melodyi-search/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-compare-mode-enhancement/02-CONTEXT.md
@.planning/phases/02-compare-mode-enhancement/02-RESEARCH.md
@.planning/phases/02-compare-mode-enhancement/02-01-PLAN.md

<interfaces>
<!-- Executor 需要的接口定义 -->

From melodyi_search/domain/services/comparison_recorder.py (Plan 02-01):
```python
class ComparisonRecorder:
    def generate_session_id(self) -> str: ...
    def write_session(self, session_id: str, request: ProviderSearchRequest) -> None: ...
    def write_provider_result(self, session_id: str, result: ProviderSearchResult) -> None: ...
    def write_search_results(self, session_id: str, provider: str, results: List[SearchResultItem]) -> None: ...
```

From melodyi_search/domain/services/execution_strategy.py (现有代码):
```python
class ExecutionStrategy:
    def execute_comparison(
        self,
        providers: List[BaseProvider],
        request: ProviderSearchRequest,
        on_provider_complete: Optional[Callable[[ProviderSearchResult], None]] = None
    ) -> UnifiedSearchResult: ...

    def _execute_background_providers(
        self,
        providers: List[BaseProvider],
        request: ProviderSearchRequest,
        on_provider_complete: Optional[Callable[[ProviderSearchResult], None]] = None
    ) -> None: ...
```

关键修改位置 (Line 128-133):
```python
thread = threading.Thread(
    target=self._execute_background_providers,
    args=(remaining_providers, request, on_provider_complete),
    daemon=True  # 需改为 daemon=False
)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 修改 execute_comparison() 集成 recorder</name>
  <files>melodyi_search/domain/services/execution_strategy.py</files>
  <read_first>
    - melodyi_search/domain/services/execution_strategy.py
    - melodyi_search/domain/services/comparison_recorder.py
    - melodyi_search/domain/models/search_result.py
  </read_first>
  <behavior>
    - Test 1: execute_comparison() 接收 recorder 参数
    - Test 2: 执行时生成 session_id 并调用 write_session()
    - Test 3: 第一个供应商完成后调用 write_provider_result() 和 write_search_results()
    - Test 4: UnifiedSearchResult 包含 session_id 字段
  </behavior>
  <action>
    修改 melodyi_search/domain/services/execution_strategy.py 的 execute_comparison() 方法：

    1. **添加 recorder 参数** (COMP-02 决策):
       ```python
       def execute_comparison(
           self,
           providers: List[BaseProvider],
           request: ProviderSearchRequest,
           recorder: ComparisonRecorder,  # 新增参数
           on_provider_complete: Optional[Callable[[ProviderSearchResult], None]] = None
       ) -> UnifiedSearchResult:
           """比对模式执行

           D-01: daemon=False + thread.join(timeout=10)
           COMP-02: 持久化到 SQLite
           COMP-06: 返回包含 session_id 的结果
           """
       ```

    2. **添加导入**:
       ```python
       from melodyi_search.domain.services.comparison_recorder import ComparisonRecorder
       ```

    3. **生成 session_id 并写入 session 元数据** (D-03 决策):
       ```python
       # 在 providers 检查后添加:
       session_id = recorder.generate_session_id()
       recorder.write_session(session_id, request)
       logger.info(f"[Comparison] Session created: {session_id}")
       ```

    4. **第一个供应商完成后立即写入** (D-02 决策):
       ```python
       # 在 first_result 执行后添加:
       recorder.write_provider_result(session_id, first_result)
       recorder.write_search_results(session_id, first_provider.name, first_result.results)
       ```

    5. **传递 session_id 和 recorder 到后台线程**:
       ```python
       thread = threading.Thread(
           target=self._execute_background_providers,
           args=(remaining_providers, request, recorder, session_id, on_provider_complete),  # 新增参数
           daemon=False  # D-01 决策
       )
       ```

    6. **等待后台线程完成** (D-01 决策):
       ```python
       # 在 thread.start() 后添加:
       if thread:
           thread.join(timeout=10)
           if thread.is_alive():
               logger.warning(f"[Comparison] Background thread still alive after 10s timeout, proceeding anyway")
       ```

    7. **返回包含 session_id 的结果** (COMP-06 决策):
       ```python
       unified_result = self._convert_to_unified_result(first_result)
       unified_result.session_id = session_id  # 添加 session_id
       ```

    **关键约束**:
    - recorder 参数为必选参数（非 Optional）
    - session_id 生成后立即写入 session 元数据
    - 第一个供应商结果立即写入数据库
    - 后台线程参数需要增加 recorder 和 session_id
    - 需要等待后台线程完成（timeout=10）
  </action>
  <verify>
    <automated>grep -c "recorder: ComparisonRecorder" melodyi_search/domain/services/execution_strategy.py | grep -q "^1$"</automated>
  </verify>
  <done>
    - execute_comparison() 接收 recorder 参数
    - session_id 在方法开始时生成
    - write_session() 被调用
    - 第一个供应商结果写入数据库
    - thread.join(timeout=10) 存在
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 修改后台线程为 daemon=False 并添加数据写入</name>
  <files>melodyi_search/domain/services/execution_strategy.py</files>
  <read_first>
    - melodyi_search/domain/services/execution_strategy.py
    - melodyi_search/domain/services/comparison_recorder.py
  </read_first>
  <behavior>
    - Test 1: 后台线程参数包含 recorder 和 session_id
    - Test 2: daemon=False 设置正确
    - Test 3: 每个供应商完成后调用 write_provider_result() 和 write_search_results()
    - Test 4: 持久化失败时继续执行下一个供应商
  </behavior>
  <action>
    修改 melodyi_search/domain/services/execution_strategy.py 的 _execute_background_providers() 方法：

    1. **修改方法签名添加参数**:
       ```python
       def _execute_background_providers(
           self,
           providers: List[BaseProvider],
           request: ProviderSearchRequest,
           recorder: ComparisonRecorder,  # 新增参数
           session_id: str,  # 新增参数
           on_provider_complete: Optional[Callable[[ProviderSearchResult], None]] = None
       ) -> None:
           """后台执行剩余供应商

           D-02: 每个供应商完成后立即写入
           D-04: 失败时日志记录继续
           """
       ```

    2. **修改线程创建为 daemon=False** (Line 128-133, D-01 决策):
       ```python
       # 原代码:
       thread = threading.Thread(
           target=self._execute_background_providers,
           args=(remaining_providers, request, on_provider_complete),
           daemon=True  # 修改为 daemon=False
       )

       # 新代码:
       thread = threading.Thread(
           target=self._execute_background_providers,
           args=(remaining_providers, request, recorder, session_id, on_provider_complete),
           daemon=False  # D-01: 非 daemon 线程确保写入完成
       )
       ```

    3. **在供应商循环中添加数据写入** (D-02 决策):
       ```python
       for provider in providers:
           provider_name = provider.name
           try:
               logger.debug(f"[Comparison] Background executing provider: {provider_name}")
               result = provider.search(request)

               # D-02: 立即写入数据库
               recorder.write_provider_result(session_id, result)
               recorder.write_search_results(session_id, provider_name, result.results)

               # 回调通知
               if on_provider_complete:
                   on_provider_complete(result)

               # 记录结果到内存字典（保持现有逻辑）
               with self._comparison_lock:
                   self._comparison_results[provider_name] = {
                       "status": "success" if result.error is None else "failed",
                       "time_ms": result.response_time_ms,
                       "results_count": len(result.results),
                       "error": result.error
                   }

               if result.error:
                   logger.info(f"[Comparison] Background provider {provider_name} failed: {result.error}")
               else:
                   logger.info(
                       f"[Comparison] Background provider {provider_name} succeeded: "
                       f"{result.response_time_ms}ms, {len(result.results)} results"
                   )

           except Exception as e:
               error_msg = str(e)
               logger.error(f"[Comparison] Background provider {provider_name} raised exception: {error_msg}")
               # D-04: 继续执行下一个供应商
               with self._comparison_lock:
                   self._comparison_results[provider_name] = {
                       "status": "exception",
                       "time_ms": 0,
                       "results_count": 0,
                       "error": error_msg
                   }
       ```

    **关键约束**:
    - daemon=False 必须设置（Line 131）
    - recorder.write_provider_result() 和 recorder.write_search_results() 在每个供应商完成后立即调用
    - 保持现有的内存字典记录逻辑（comparison_log 仍可用）
    - 持久化失败不影响下一个供应商执行（D-04）
  </action>
  <verify>
    <automated>grep -c "daemon=False" melodyi_search/domain/services/execution_strategy.py | grep -q "^1$"</automated>
  </verify>
  <done>
    - daemon=False 设置正确
    - _execute_background_providers() 参数包含 recorder 和 session_id
    - write_provider_result() 在供应商完成后调用
    - write_search_results() 在供应商完成后调用
    - thread.join(timeout=10) 存在
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 添加集成测试验证持久化流程</name>
  <files>tests/domain/services/test_execution_strategy.py</files>
  <read_first>
    - tests/domain/services/test_execution_strategy.py
    - melodyi_search/domain/services/execution_strategy.py
    - melodyi_search/domain/services/comparison_recorder.py
    - tests/infrastructure/database/test_database_manager.py (Phase 1 测试模式)
  </read_first>
  <behavior>
    - Test 1: execute_comparison() 生成 session_id 并写入数据库
    - Test 2: 后台线程完成后数据写入数据库
    - Test 3: thread.join(timeout=10) 确保线程等待
    - Test 4: UnifiedSearchResult 包含 session_id
    - Test 5: daemon=False 确保数据不丢失
  </behavior>
  <action>
    修改 tests/domain/services/test_execution_strategy.py，添加集成测试：

    1. **添加测试类 TestComparisonPersistence**:
       ```python
       import pytest
       import tempfile
       import os
       import time
       from unittest.mock import Mock, MagicMock
       from melodyi_search.domain.services.execution_strategy import ExecutionStrategy
       from melodyi_search.domain.services.comparison_recorder import ComparisonRecorder
       from melodyi_search.infrastructure.database.database_manager import DatabaseManager
       from melodyi_search.infrastructure.config.config_schema import DatabaseConfig
       from melodyi_search.providers.base_provider import BaseProvider, ProviderSearchRequest, ProviderSearchResult, SearchResultItem

       class TestComparisonPersistence:
           """集成测试: Compare 模式持久化"""

           @pytest.fixture
           def temp_db(self):
               """创建临时数据库"""
               with tempfile.NamedTemporaryFile(suffix='.db', delete=False) as f:
                   db_path = f.name
               config = DatabaseConfig(database_path=db_path)
               manager = DatabaseManager(config)
               manager.init_database()
               yield manager
               os.unlink(db_path)

           @pytest.fixture
           def recorder(self, temp_db):
               """创建 recorder 实例"""
               return ComparisonRecorder(temp_db)

           @pytest.fixture
           def strategy(self):
               """创建执行策略实例"""
               return ExecutionStrategy()

           @pytest.fixture
           def mock_provider(self):
               """创建 Mock provider"""
               provider = Mock(spec=BaseProvider)
               provider.name = "test_provider"
               provider.search = Mock(return_value=ProviderSearchResult(
                   provider="test_provider",
                   results=[
                       SearchResultItem(title="Test", url="https://example.com", description="Test result")
                   ],
                   response_time_ms=100,
                   error=None
               ))
               return provider

           def test_execute_comparison_persists_session(self, strategy, recorder, temp_db, mock_provider):
               """测试 session 写入数据库"""
               request = ProviderSearchRequest(query="test", max_results=10)
               result = strategy.execute_comparison(
                   providers=[mock_provider],
                   request=request,
                   recorder=recorder
               )

               # 验证 session_id 存在
               assert result.session_id is not None

               # 验证数据库写入
               conn = temp_db.get_connection()
               session = conn.execute(
                   "SELECT session_id, query FROM comparison_sessions WHERE session_id=?",
                   (result.session_id,)
               ).fetchone()
               conn.close()
               assert session is not None
               assert session[1] == "test"

           def test_daemon_thread_waits_for_completion(self, strategy, recorder, temp_db):
               """测试 daemon=False + join(timeout) 确保写入"""
               # 创建两个 mock providers
               provider1 = Mock(spec=BaseProvider)
               provider1.name = "provider1"
               provider1.search = Mock(return_value=ProviderSearchResult(
                   provider="provider1",
                   results=[SearchResultItem(title="P1", url="https://p1.com", description="")],
                   response_time_ms=50,
                   error=None
               ))

               provider2 = Mock(spec=BaseProvider)
               provider2.name = "provider2"
               provider2.search = Mock(return_value=ProviderSearchResult(
                   provider="provider2",
                   results=[SearchResultItem(title="P2", url="https://p2.com", description="")],
                   response_time_ms=50,
                   error=None
               ))

               request = ProviderSearchRequest(query="test", max_results=10)
               result = strategy.execute_comparison(
                   providers=[provider1, provider2],
                   request=request,
                   recorder=recorder
               )

               # 等待后台线程完成（strategy 内部已等待）
               # 验证两个供应商结果都已写入
               conn = temp_db.get_connection()
               providers_written = conn.execute(
                   "SELECT provider FROM provider_results WHERE session_id=?",
                   (result.session_id,)
               ).fetchall()
               conn.close()
               assert len(providers_written) == 2
       ```

    2. **覆盖 COMP-02, COMP-07 需求**:
       - test_execute_comparison_persists_session (COMP-02)
       - test_daemon_thread_waits_for_completion (COMP-07)
       - test_result_contains_session_id (COMP-06 验证)

    文件位置: tests/domain/services/test_execution_strategy.py
    新增测试数: 3-4 个
  </action>
  <verify>
    <automated>pytest tests/domain/services/test_execution_strategy.py -v --tb=short</automated>
  </verify>
  <done>
    - TestComparisonPersistence 类定义
    - test_execute_comparison_persists_session 通过
    - test_daemon_thread_waits_for_completion 通过
    - 所有测试通过
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Domain Service → Database | 本地 SQLite 操作，无外部暴露 |
| Background Thread → Main Thread | 线程间共享 recorder 和 session_id |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Tampering | thread.join(timeout) | accept | timeout 后继续退出，保障可用性 |
| T-02-04 | Denial of Service | thread.join(timeout=10) | mitigate | timeout 限制等待时间，避免阻塞 |
</threat_model>

<verification>
## Per-Task Verification

| Task | Automated Command | Status |
|------|-------------------|--------|
| Task 1 | `grep -c "recorder: ComparisonRecorder" melodyi_search/domain/services/execution_strategy.py` | ⬜ pending |
| Task 2 | `grep -c "daemon=False" melodyi_search/domain/services/execution_strategy.py` | ⬜ pending |
| Task 3 | `pytest tests/domain/services/test_execution_strategy.py -v` | ⬜ pending |

## Wave Completion

Wave 2 完成后运行:
```bash
pytest tests/domain/services/ -v
```
</verification>

<success_criteria>
- execute_comparison() 接收 recorder 参数
- daemon=False 设置正确
- thread.join(timeout=10) 存在
- 后台线程完成后数据写入数据库
- 集成测试全部通过
</success_criteria>

<output>
完成后创建 `.planning/phases/02-compare-mode-enhancement/02-02-SUMMARY.md`
</output>
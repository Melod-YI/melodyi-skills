---
phase: 05-integration-testing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [INT-01, INT-02, INT-03, INT-04]
user_setup: []

must_haves:
  truths:
    - "所有现有 446 tests 通过"
    - "Normal Search 模式（comparison OFF）输出格式与 Phase 01-02 前一致"
    - "skill.md 参数定义不变，Agent 使用方式不变"
    - "配置文件向后兼容，现有配置无需修改"
    - "数据库作为可选组件，关闭 comparison 时不初始化"
  artifacts:
    - path: "tests/"
      provides: "回归测试验证"
      min_lines: 1
    - path: "skill.md"
      provides: "Agent 接口定义"
      contains: "query"
    - path: "melodyi_search/infrastructure/config/default_config.yaml"
      provides: "配置文件"
      contains: "database:"
  key_links:
    - from: "CLI normal mode"
      to: "execute_normal()"
      via: "comparison=False"
      pattern: "use_comparison = False"
    - from: "skill.md"
      to: "Agent"
      via: "参数定义"
      pattern: "query.*string.*是"
---

<objective>
执行完整回归测试验证，确保 Phase 01-03 的增强不影响现有 Normal Search 功能和 Agent 集成接口。

Purpose: 系统集成验证，确认 Compare 模式作为可选增强，不影响主体功能。
Output: 回归测试报告 + 集成验证确认。
</objective>

<execution_context>
@C:/workspace/melodyi-skills/skills/melodyi-search/.claude/get-shit-done/workflows/execute-plan.md
@C:/workspace/melodyi-skills/skills/melodyi-search/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-integration-testing/05-CONTEXT.md
@.planning/codebase/TESTING.md
@.planning/codebase/ARCHITECTURE.md

# Phase 03 验证结果
@.planning/phases/03-cli-commands/03-VERIFICATION.md

<interfaces>
<!-- Key interfaces for regression verification -->

From skill.md:
```markdown
| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| query | string | 是 | 搜索查询关键词 |
| max_results | int | 否 | 期望返回的最大结果数，默认 10 |
| time_range | object | 否 | 时间过滤配置 |
| include_domains | array | 否 | 仅搜索指定域名列表 |
| exclude_domains | array | 否 | 排除指定域名列表 |
| language | string | 否 | ISO 语言代码 |
```

From default_config.yaml:
```yaml
mode:
  comparison: true
  log_dir: ./logs

database:
  database_path: ./data/compare.db
```

From cli.py (Phase 03 implemented):
```python
# D-05: CLI --comparison parameter overrides config.mode.comparison
use_comparison = comparison or config.mode.comparison
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 执行完整回归测试验证</name>
  <files>无文件修改（仅运行测试）</files>
  <read_first>
    .planning/codebase/TESTING.md
    tests/application/test_cli.py
    tests/domain/services/test_execution_strategy.py
  </read_first>
  <action>
运行完整测试套件验证所有现有功能正常工作。

执行命令:
```bash
pytest tests/ -v --tb=short
```

验证标准:
1. 所有 446 tests 通过
2. 无新增失败测试
3. 无新增警告或 deprecated 信息

如果测试失败，记录失败原因并报告给 orchestrator，不自动修复（回归测试失败意味着 Phase 01-03 引入了破坏性变更）。

收集测试结果统计:
- 总测试数
- 通过数
- 失败数（应为 0）
- 跳过数
</action>
  <verify>
    <automated>pytest tests/ --tb=no -q 2>&1 | grep "passed"</automated>
    Expected: "446 passed" 或类似格式
  </verify>
  <done>
    - 所有 446 tests 通过
    - 无失败测试
    - 测试报告记录完成
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Normal Search 模式回归验证</name>
  <files>无文件修改（仅运行测试）</files>
  <read_first>
    melodyi_search/application/cli.py
    tests/application/test_cli.py
  </read_first>
  <action>
验证 Normal Search 模式（comparison 关闭）功能与 Phase 01-02 前一致。

执行验证:
1. 运行 CLI 单元测试验证 Normal 模式:
```bash
pytest tests/application/test_cli.py -v -k "not comparison"
```

2. 验证配置关闭 comparison 时的行为:
```bash
pytest tests/application/test_cli.py -v -k "config_enabled"
```

3. 手动验证 CLI 输出格式（通过测试断言确认):
- 输出包含 provider, response_time_ms, results
- 输出不包含 session_id（Normal 模式无此字段）
- 输出不包含 comparison_log

关键断言验证点:
- `test_cli_output_format_no_session_id` 确认 session_id 不输出
- `test_cli_comparison_vs_normal_output_identical` 确认 Compare 输出与 Normal 一致
- Normal 模式测试确认无 database 初始化

记录验证结果:
- Normal 模式测试通过数
- 配置覆盖测试通过数
- 输出格式验证通过
</action>
  <verify>
    <automated>pytest tests/application/test_cli.py -v --tb=no 2>&1 | grep -E "(passed|PASSED)"</automated>
    Expected: "22 passed"
  </verify>
  <done>
    - Normal Search 模式所有测试通过
    - 输出格式验证符合 D-03/D-06/D-07
    - 配置关闭 comparison 时无 database 初始化
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Provider API 连通性回归验证</name>
  <files>无文件修改（仅运行测试）</files>
  <read_first>
    tests/integration/test_tavily_e2e.py
    tests/integration/test_brave_e2e.py
    tests/integration/test_exa_e2e.py
    tests/integration/test_minimax_cn_e2e.py
  </read_first>
  <action>
验证 Provider E2E 测试状态，确认 API 连通性。

执行验证:
1. 收集 E2E 测试文件列表:
```bash
ls tests/integration/test_*_e2e.py
```

2. 运行 Provider 单元测试（不调用真实 API）:
```bash
pytest tests/providers/ -v --tb=no
```

3. 检查 E2E 测试状态（记录是否需要真实 API Key）:
- E2E 测试依赖环境变量 API Key
- 如果环境变量缺失，E2E 测试会被跳过（正常行为）
- 记录跳过状态作为验证结果

Provider 覆盖验证:
- minimax-cn: 已有 test_minimax_cn_provider.py + test_minimax_cn_e2e.py
- tavily: 已有 test_tavily_provider.py + test_tavily_e2e.py
- brave: 已有 test_brave_provider.py + test_brave_e2e.py
- exa: 已有 test_exa_provider.py + test_exa_e2e.py

记录:
- Provider 单元测试通过数
- E2E 测试状态（passed/skipped）
- API Key 配置状态
</action>
  <verify>
    <automated>pytest tests/providers/ -v --tb=no -q 2>&1 | grep -E "(passed|skipped)"</automated>
    Expected: Provider tests pass (may have skipped E2E tests)
  </verify>
  <done>
    - 所有 Provider 单元测试通过
    - E2E 测试状态记录完成
    - Provider API 连通性验证通过（单元测试层面）
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 无新增边界 | 此 Phase 为验证性回归，不引入新功能或新边界 |

## STRIDE Threat Register

此 Phase 为回归测试验证，不涉及新功能开发，无需新增威胁分析。

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| N/A | - | - | - | 回归验证 Phase，无新增威胁 |
</threat_model>

<verification>
## Phase 5 验证标准

### INT-01 验证: Normal Search 模式不变
- 所有 CLI 测试通过（22 tests）
- Normal 模式输出格式不变
- 配置关闭 comparison 时行为不变

### INT-02 验证: skill.md 不变
- skill.md 参数定义不变
- Agent 使用方式不变
- 输出格式符合 skill.md 描述

### INT-03 验证: 配置文件不变
- default_config.yaml 格式向后兼容
- 现有配置项无删除
- 新增 database 配置项可选

### INT-04 验证: 数据库路径配置项
- database.database_path 配置项已存在
- 默认值 ./data/compare.db
- 配置关闭 comparison 时数据库不初始化

### 自动化验证命令
```bash
# 全量回归测试
pytest tests/ -v --tb=short

# CLI Normal 模式测试
pytest tests/application/test_cli.py -v -k "not comparison"

# Provider 测试
pytest tests/providers/ -v
```
</verification>

<success_criteria>
- [ ] 所有 446 tests 通过
- [ ] Normal Search 模式测试通过
- [ ] Provider 单元测试通过
- [ ] skill.md 参数定义不变
- [ ] 配置文件向后兼容
- [ ] 数据库为可选组件验证通过
- [ ] 回归测试报告完成
</success_criteria>

<output>
After completion, create `.planning/phases/05-integration-testing/05-01-SUMMARY.md`

此 Phase 为主回归验证，如所有测试通过，Phase 5 完成。如有失败，报告给 orchestrator 进行问题分析。
</output>
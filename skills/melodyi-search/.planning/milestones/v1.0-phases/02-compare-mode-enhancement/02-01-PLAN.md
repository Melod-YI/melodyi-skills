---
phase: 02-compare-mode-enhancement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - melodyi_search/domain/services/comparison_recorder.py
  - tests/domain/services/test_comparison_recorder.py
autonomous: true
requirements: [COMP-01, COMP-03, COMP-04, COMP-05]
user_setup: []

must_haves:
  truths:
    - "ComparisonRecorder 服务可被导入"
    - "session_id 格式符合 YYYYMMDD-HHMMSS-XXXX"
    - "数据写入方法存在且参数正确"
    - "持久化失败时记录日志继续执行"
  artifacts:
    - path: "melodyi_search/domain/services/comparison_recorder.py"
      provides: "对比数据记录服务"
      min_lines: 120
      exports: ["ComparisonRecorder", "generate_session_id", "write_session", "write_provider_result", "write_search_results"]
    - path: "tests/domain/services/test_comparison_recorder.py"
      provides: "单元测试"
      contains: "class TestComparisonRecorder"
  key_links:
    - from: "melodyi_search/domain/services/comparison_recorder.py"
      to: "melodyi_search/infrastructure/database/database_manager.py"
      via: "DatabaseManager.get_connection()"
      pattern: "get_connection()"
---

<objective>
创建 ComparisonRecorder 领域服务，实现对比数据记录功能

Purpose: 为 Compare 模式提供数据持久化服务，记录完整搜索结果、请求参数和元数据
Output: comparison_recorder.py + 单元测试
</objective>

<execution_context>
@C:/workspace/melodyi-skills/skills/melodyi-search/.claude/get-shit-done/workflows/execute-plan.md
@C:/workspace/melodyi-skills/skills/melodyi-search/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-compare-mode-enhancement/02-CONTEXT.md
@.planning/phases/02-compare-mode-enhancement/02-RESEARCH.md

<interfaces>
<!-- Executor 需要的接口定义 -->

From melodyi_search/infrastructure/database/database_manager.py:
```python
class DatabaseManager:
    def get_connection(self) -> sqlite3.Connection: ...
    def init_database(self) -> None: ...
```

From melodyi_search/providers/base_provider.py:
```python
class ProviderSearchRequest:
    query: str
    max_results: int
    time_range: Optional[TimeRange]
    include_domains: Optional[List[str]]
    exclude_domains: Optional[List[str]]
    language: Optional[str]

class ProviderSearchResult:
    provider: str
    results: List[SearchResultItem]
    response_time_ms: int
    error: Optional[str]

class SearchResultItem:
    title: str
    url: str
    description: str
    published_date: Optional[datetime]
    source_domain: str
```

From melodyi_search/domain/models/search_request.py:
```python
class TimeRange:
    range_type: str  # "day", "week", "month", "year"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 创建 ComparisonRecorder 服务骨架</name>
  <files>melodyi_search/domain/services/comparison_recorder.py</files>
  <read_first>
    - melodyi_search/infrastructure/database/database_manager.py
    - melodyi_search/providers/base_provider.py
    - melodyi_search/domain/models/search_result.py
  </read_first>
  <behavior>
    - Test 1: ComparisonRecorder 可被导入
    - Test 2: generate_session_id() 返回格式为 YYYYMMDD-HHMMSS-XXXX
    - Test 3: session_id 包含时间戳前缀和 4 位十六进制随机数
  </behavior>
  <action>
    创建 melodyi_search/domain/services/comparison_recorder.py，实现以下内容：

    1. **类结构定义** (D-02, D-03, D-04 决策):
       - `class ComparisonRecorder` — 对比数据记录服务
       - 构造函数接收 `DatabaseManager` 参数
       - 日志 logger = logging.getLogger(__name__)

    2. **session_id 生成方法** (D-03 决策):
       ```python
       def generate_session_id(self) -> str:
           """生成 Session ID

           D-03: 格式 YYYYMMDD-HHMMSS-XXXX
           - 时间戳前缀便于历史查询和日志追溯
           - 4 位十六进制随机数防止冲突
           """
           timestamp = datetime.now().strftime("%Y%m%d-%H%M%S")
           random_suffix = ''.join(random.choices('0123456789abcdef', k=4))
           return f"{timestamp}-{random_suffix}"
       ```

    3. **导入依赖**:
       - `import json`, `import logging`, `import random`, `from datetime import datetime`
       - `from melodyi_search.infrastructure.database.database_manager import DatabaseManager`
       - `from melodyi_search.providers.base_provider import ProviderSearchRequest, ProviderSearchResult, SearchResultItem`

    4. **日志规范** (Phase 1 模式):
       - INFO: 数据写入成功
       - ERROR: 数据写入失败 (D-04 决策)
       - DEBUG: 连接打开/关闭

    文件位置: melodyi_search/domain/services/comparison_recorder.py
    估计行数: ~150 行（含完整实现）
  </action>
  <verify>
    <automated>python -c "from melodyi_search.domain.services.comparison_recorder import ComparisonRecorder; print('Import OK')"</automated>
  </verify>
  <done>
    - ComparisonRecorder 类可被导入
    - generate_session_id() 方法存在
    - 日志 logger 已初始化
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现数据写入方法</name>
  <files>melodyi_search/domain/services/comparison_recorder.py</files>
  <read_first>
    - melodyi_search/domain/services/comparison_recorder.py (Task 1 创建的骨架)
    - melodyi_search/infrastructure/database/database_manager.py
  </read_first>
  <behavior>
    - Test 1: write_session() 成功写入 comparison_sessions 表
    - Test 2: write_provider_result() 成功写入 provider_results 表
    - Test 3: write_search_results() 成功写入 search_results 表，rank 正确
    - Test 4: 持久化失败时记录 ERROR 日志，不抛出异常
  </behavior>
  <action>
    在 comparison_recorder.py 中添加三个数据写入方法：

    1. **write_session() 方法** (COMP-03 决策):
       ```python
       def write_session(self, session_id: str, request: ProviderSearchRequest) -> None:
           """写入 session 元数据

           D-02: 单条 autocommit
           D-04: 失败时日志记录继续执行
           """
           try:
               params_json = json.dumps({
                   "max_results": request.max_results,
                   "time_range": request.time_range.range_type if request.time_range else None,
                   "include_domains": request.include_domains,
                   "exclude_domains": request.exclude_domains,
                   "language": request.language
               }, ensure_ascii=False)

               conn = self._db.get_connection()
               conn.execute(
                   """INSERT INTO comparison_sessions
                      (session_id, query, params, timestamp)
                      VALUES (?, ?, ?, ?)""",
                   (session_id, request.query, params_json, datetime.now().timestamp())
               )
               conn.commit()
               conn.close()
               logger.info(f"Session written: {session_id}")
           except Exception as e:
               logger.error(f"Failed to write session {session_id}: {e}")
               # D-04: 不抛出异常，继续执行
       ```

    2. **write_provider_result() 方法** (COMP-05 决策):
       ```python
       def write_provider_result(self, session_id: str, result: ProviderSearchResult) -> None:
           """写入供应商结果

           记录: response_time_ms, results_count, error_type, error_message, status
           """
           try:
               status = "success" if result.error is None else "error"
               error_type = None  # 可扩展为具体错误类型
               error_message = result.error

               conn = self._db.get_connection()
               conn.execute(
                   """INSERT INTO provider_results
                      (session_id, provider, response_time_ms, results_count,
                       error_type, error_message, status)
                      VALUES (?, ?, ?, ?, ?, ?, ?)""",
                   (session_id, result.provider, result.response_time_ms,
                    len(result.results), error_type, error_message, status)
               )
               conn.commit()
               conn.close()
               logger.info(f"Provider result written: {session_id}/{result.provider}")
           except Exception as e:
               logger.error(f"Failed to write provider result {session_id}/{result.provider}: {e}")
       ```

    3. **write_search_results() 方法** (COMP-01, COMP-04 决策):
       ```python
       def write_search_results(self, session_id: str, provider: str, results: List[SearchResultItem]) -> None:
           """写入搜索结果详情

           COMP-01: 记录完整搜索结果 (title, url, description, published_date)
           COMP-04: 记录排序位置 rank 字段
           """
           try:
               conn = self._db.get_connection()
               for rank, item in enumerate(results, start=1):
                   conn.execute(
                       """INSERT INTO search_results
                          (session_id, provider, rank, title, url, description,
                           published_date, source_domain)
                          VALUES (?, ?, ?, ?, ?, ?, ?, ?)""",
                       (session_id, provider, rank, item.title, item.url,
                        item.description,
                        item.published_date.isoformat() if item.published_date else None,
                        item.source_domain)
                   )
               conn.commit()
               conn.close()
               logger.info(f"Search results written: {session_id}/{provider} ({len(results)} items)")
           except Exception as e:
               logger.error(f"Failed to write search results {session_id}/{provider}: {e}")
       ```

    **关键约束**:
    - 使用 `enumerate(results, start=1)` 生成 rank (COMP-04)
    - 所有写入方法捕获异常后记录 ERROR 日志，不抛出异常 (D-04)
    - 单条写入使用 autocommit (D-02)
    - 每次操作新建连接，完成后关闭 (Phase 1 D-02 决策)
  </action>
  <verify>
    <automated>grep -c "def write_session\|def write_provider_result\|def write_search_results" melodyi_search/domain/services/comparison_recorder.py | grep -q "^3$"</automated>
  </verify>
  <done>
    - write_session() 方法存在且写入 comparison_sessions 表
    - write_provider_result() 方法存在且写入 provider_results 表
    - write_search_results() 方法存在且写入 search_results 表
    - 所有方法捕获异常并记录 ERROR 日志
    - rank 使用 enumerate(results, start=1) 生成
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 创建单元测试骨架</name>
  <files>tests/domain/services/test_comparison_recorder.py</files>
  <read_first>
    - tests/infrastructure/database/test_database_manager.py (Phase 1 测试模式参考)
    - melodyi_search/domain/services/comparison_recorder.py
  </read_first>
  <behavior>
    - Test 1: ComparisonRecorder 类可被初始化
    - Test 2: generate_session_id() 格式正确
    - Test 3: write_session() 写入成功
    - Test 4: write_provider_result() 写入成功
    - Test 5: write_search_results() 写入成功且 rank 正确
    - Test 6: 持久化失败不抛出异常
  </behavior>
  <action>
    创建 tests/domain/services/test_comparison_recorder.py，实现以下测试类：

    1. **测试类结构** (参考 Phase 1 test_database_manager.py 模式):
       ```python
       import pytest
       import tempfile
       import os
       from datetime import datetime
       from melodyi_search.domain.services.comparison_recorder import ComparisonRecorder
       from melodyi_search.infrastructure.database.database_manager import DatabaseManager
       from melodyi_search.infrastructure.config.config_schema import DatabaseConfig
       from melodyi_search.providers.base_provider import ProviderSearchRequest, ProviderSearchResult, SearchResultItem

       class TestComparisonRecorder:
           """ComparisonRecorder 单元测试"""

           @pytest.fixture
           def temp_db(self):
               """创建临时数据库"""
               with tempfile.NamedTemporaryFile(suffix='.db', delete=False) as f:
                   db_path = f.name
               config = DatabaseConfig(database_path=db_path)
               manager = DatabaseManager(config)
               manager.init_database()
               yield manager
               os.unlink(db_path)

           @pytest.fixture
           def recorder(self, temp_db):
               """创建 recorder 实例"""
               return ComparisonRecorder(temp_db)

           def test_generate_session_id_format(self, recorder):
               """测试 session_id 格式 (D-03)"""
               session_id = recorder.generate_session_id()
               assert session_id.count('-') == 2
               parts = session_id.split('-')
               assert len(parts[0]) == 8  # YYYYMMDD
               assert len(parts[1]) == 6  # HHMMSS
               assert len(parts[2]) == 4  # XXXX (十六进制)

           def test_write_session_success(self, recorder, temp_db):
               """测试 session 写入成功"""
               session_id = recorder.generate_session_id()
               request = ProviderSearchRequest(
                   query="test query",
                   max_results=10,
                   time_range=None,
                   include_domains=None,
                   exclude_domains=None,
                   language="zh"
               )
               recorder.write_session(session_id, request)

               # 验证写入
               conn = temp_db.get_connection()
               result = conn.execute(
                   "SELECT session_id, query FROM comparison_sessions WHERE session_id=?",
                   (session_id,)
               ).fetchone()
               conn.close()
               assert result is not None
               assert result[0] == session_id
               assert result[1] == "test query"
       ```

    2. **覆盖 COMP-01, COMP-03, COMP-04, COMP-05 需求**:
       - test_write_session_success (COMP-03)
       - test_write_provider_result_success (COMP-05)
       - test_write_search_results_with_rank (COMP-01, COMP-04)
       - test_persistence_failure_no_exception (D-04)

    3. **测试模式**:
       - 使用 tempfile 创建临时数据库
       - fixture 管理 recorder 和 temp_db
       - pytest 标准断言

    文件位置: tests/domain/services/test_comparison_recorder.py
    估计测试数: 6-8 个
  </action>
  <verify>
    <automated>pytest tests/domain/services/test_comparison_recorder.py -v --tb=short</automated>
  </verify>
  <done>
    - 测试文件存在
    - TestComparisonRecorder 类定义
    - generate_session_id 格式测试通过
    - write_session 测试通过
    - write_provider_result 测试通过
    - write_search_results 测试通过 (含 rank 验证)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Domain Service → Database | 本地 SQLite 操作，无外部暴露 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | write_session() | mitigate | 参数化查询防止 SQL 注入 |
| T-02-02 | Information Disclosure | logger.error() | accept | 日志不包含 API 密钥等敏感信息 |
</threat_model>

<verification>
## Per-Task Verification

| Task | Automated Command | Status |
|------|-------------------|--------|
| Task 1 | `python -c "from melodyi_search.domain.services.comparison_recorder import ComparisonRecorder"` | ⬜ pending |
| Task 2 | `grep -c "def write_session" melodyi_search/domain/services/comparison_recorder.py` | ⬜ pending |
| Task 3 | `pytest tests/domain/services/test_comparison_recorder.py -v` | ⬜ pending |

## Wave Completion

Wave 1 完成后运行:
```bash
pytest tests/domain/services/test_comparison_recorder.py -v
```
</verification>

<success_criteria>
- ComparisonRecorder 类可被导入并初始化
- generate_session_id() 返回格式为 YYYYMMDD-HHMMSS-XXXX
- 三个写入方法存在且参数正确
- 单元测试全部通过
- 持久化失败时不抛出异常，记录 ERROR 日志
</success_criteria>

<output>
完成后创建 `.planning/phases/02-compare-mode-enhancement/02-01-SUMMARY.md`
</output>
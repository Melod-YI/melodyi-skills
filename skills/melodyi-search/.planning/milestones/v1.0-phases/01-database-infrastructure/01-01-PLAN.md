---
phase: 01-database-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - melodyi_search/infrastructure/config/config_schema.py
  - melodyi_search/infrastructure/config/default_config.yaml
  - melodyi_search/infrastructure/database/__init__.py
  - melodyi_search/infrastructure/database/database_manager.py
autonomous: true
requirements:
  - DB-01
  - DB-02
  - DB-03
  - DB-04
  - DB-05
must_haves:
  truths:
    - "数据库管理器模块可以被导入"
    - "数据库文件在配置路径自动创建"
    - "三张表结构完整创建"
    - "索引正确建立"
    - "初始化操作幂等可重复执行"
  artifacts:
    - path: "melodyi_search/infrastructure/database/database_manager.py"
      provides: "数据库连接管理、表创建、索引创建"
      min_lines: 80
    - path: "melodyi_search/infrastructure/database/__init__.py"
      provides: "模块导出 DatabaseManager"
      exports: ["DatabaseManager"]
    - path: "melodyi_search/infrastructure/config/config_schema.py"
      provides: "扩展配置模型，添加 database 字段"
      contains: "class DatabaseConfig"
    - path: "melodyi_search/infrastructure/config/default_config.yaml"
      provides: "默认数据库配置"
      contains: "database:"
  key_links:
    - from: "melodyi_search/infrastructure/database/database_manager.py"
      to: "melodyi_search/infrastructure/config/config_schema.py"
      via: "DatabaseConfig.database_path"
      pattern: "config\\.database\\.database_path"
    - from: "database_manager.py:init_database()"
      to: "./data/compare.db"
      via: "sqlite3.connect()"
      pattern: "sqlite3\\.connect"
---

<objective>
建立 SQLite 持久化基础设施，实现数据库连接管理、表结构创建和索引建立。

Purpose: 为 Phase 2 的对比数据持久化提供底层数据库支持，确保数据存储基础设施完整。
Output: 可用的数据库管理器模块、扩展的配置模型、完整表结构。

**用户决策实现:**
- D-01: Lazy initialization — CLI 启动时自动检查并创建数据库
- D-02: 连接管理 — 使用 sqlite3 标准库，每次操作新建连接
- D-03: 事务策略 — 单条插入 autocommit，批量显式事务 (Phase 2 实现)
- D-04: 文件位置 — 默认 `./data/compare.db`，通过配置文件可自定义
- D-05: 表命名 — snake_case
</objective>

<execution_context>
@C:/workspace/melodyi-skills/skills/melodyi-search/.claude/get-shit-done/workflows/execute-plan.md
@C:/workspace/melodyi-skills/skills/melodyi-search/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-database-infrastructure/01-CONTEXT.md

<interfaces>
<!-- 现有配置模式，executor 需要扩展 -->
From melodyi_search/infrastructure/config/config_schema.py:
```python
class ModeConfig(BaseModel):
    comparison: bool = Field(default=False, description="是否开启比对模式")
    log_dir: str = Field(default="./logs", description="日志目录")

class Config(BaseModel):
    providers: List[ProviderConfig] = Field(..., description="提供商配置数组")
    mode: ModeConfig = Field(default_factory=ModeConfig, description="运行模式")
    fallback: FallbackConfig = Field(default_factory=FallbackConfig, description="回退配置")
```

<!-- 日志模式参考 -->
From melodyi_search/infrastructure/logging/search_logger.py:
```python
self.logger = logging.getLogger(self._logger_name)
self.logger.setLevel(logging.DEBUG)
self.logger.info(f"Provider: {provider} | Status: success | Time: {time_ms}ms")
```
</interfaces>
</context>

<tasks>

<task id="1" type="auto">
<name>Task 1: 扩展配置模型添加数据库配置</name>
<files>
  - melodyi_search/infrastructure/config/config_schema.py
  - melodyi_search/infrastructure/config/default_config.yaml
</files>
<read_first>
  - melodyi_search/infrastructure/config/config_schema.py
  - melodyi_search/infrastructure/config/default_config.yaml
</read_first>
<action>
**实现 D-04: 数据库文件位置配置**

1. 在 `config_schema.py` 中添加 `DatabaseConfig` Pydantic 模型:
```python
class DatabaseConfig(BaseModel):
    """数据库配置"""
    database_path: str = Field(
        default="./data/compare.db",
        description="SQLite 数据库文件路径"
    )
```

2. 在 `Config` 类中添加 `database` 字段:
```python
class Config(BaseModel):
    providers: List[ProviderConfig] = Field(..., description="提供商配置数组")
    mode: ModeConfig = Field(default_factory=ModeConfig, description="运行模式")
    fallback: FallbackConfig = Field(default_factory=FallbackConfig, description="回退配置")
    database: DatabaseConfig = Field(default_factory=DatabaseConfig, description="数据库配置")
```

3. 在 `default_config.yaml` 添加数据库配置节:
```yaml
database:
  database_path: ./data/compare.db
```

**遵循现有 Pydantic V2 模式:**
- 使用 `Field(default=..., description="...")` 格式
- 使用 `default_factory` 对于复杂类型
- 中文文档字符串
</action>
<acceptance_criteria>
- `grep "class DatabaseConfig" melodyi_search/infrastructure/config/config_schema.py` 返回匹配
- `grep "database_path" melodyi_search/infrastructure/config/config_schema.py` 返回匹配
- `grep "database:" melodyi_search/infrastructure/config/default_config.yaml` 返回匹配
- `grep "database_path: ./data/compare.db" melodyi_search/infrastructure/config/default_config.yaml` 返回匹配
</acceptance_criteria>
<done>
DatabaseConfig 类定义完成，Config 类包含 database 字段，default_config.yaml 包含数据库配置节。
</done>
</task>

<task id="2" type="auto">
<name>Task 2: 创建数据库管理器模块</name>
<files>
  - melodyi_search/infrastructure/database/__init__.py
  - melodyi_search/infrastructure/database/database_manager.py
</files>
<read_first>
  - melodyi_search/infrastructure/config/config_schema.py (查看 DatabaseConfig)
  - melodyi_search/infrastructure/logging/search_logger.py (日志模式参考)
</read_first>
<action>
**创建 `melodyi_search/infrastructure/database/` 目录和核心模块**

1. 创建 `__init__.py`:
```python
"""数据库基础设施模块"""

from melodyi_search.infrastructure.database.database_manager import DatabaseManager

__all__ = ["DatabaseManager"]
```

2. 创建 `database_manager.py`，实现以下功能:

```python
"""数据库管理器 - SQLite 连接和表管理"""

import sqlite3
import logging
from pathlib import Path
from typing import Optional
from melodyi_search.infrastructure.config.config_schema import DatabaseConfig


class DatabaseManager:
    """SQLite 数据库管理器
    
    负责:
    - 数据库文件创建和连接管理 (D-02: 每次操作新建连接)
    - 表结构初始化 (D-01: lazy initialization)
    - 索引创建
    """
    
    def __init__(self, config: DatabaseConfig):
        """初始化数据库管理器
        
        Args:
            config: 数据库配置，包含 database_path
        """
        self.config = config
        self.database_path = Path(config.database_path)
        self._logger = logging.getLogger("melodyi_search.database")
        
        # 确保数据目录存在
        self.database_path.parent.mkdir(parents=True, exist_ok=True)
    
    def get_connection(self) -> sqlite3.Connection:
        """获取数据库连接
        
        D-02: 每次操作新建连接，SQLite 轻量级场景无需连接池
        
        Returns:
            sqlite3.Connection: 数据库连接对象
        """
        conn = sqlite3.connect(str(self.database_path))
        # 启用外键约束
        conn.execute("PRAGMA foreign_keys = ON")
        self._logger.debug(f"Database connection opened: {self.database_path}")
        return conn
    
    def init_database(self) -> None:
        """初始化数据库 - 创建表结构和索引
        
        D-01: Lazy initialization，幂等操作，可重复执行
        D-05: 表命名使用 snake_case
        """
        self._logger.info(f"Initializing database at {self.database_path}")
        
        conn = self.get_connection()
        try:
            self._create_tables(conn)
            self._create_indexes(conn)
            self._logger.info("Database initialization completed successfully")
        except Exception as e:
            self._logger.error(f"Database initialization failed: {e}")
            raise
        finally:
            conn.close()
    
    def _create_tables(self, conn: sqlite3.Connection) -> None:
        """创建表结构
        
        表设计:
        - comparison_sessions: 对比执行会话
        - provider_results: 供应商执行结果
        - search_results: 搜索结果详情
        """
        # comparison_sessions 表 (DB-02)
        conn.execute("""
            CREATE TABLE IF NOT EXISTS comparison_sessions (
                session_id TEXT PRIMARY KEY,
                query TEXT NOT NULL,
                params TEXT NOT NULL,  -- JSON 格式存储请求参数
                timestamp REAL NOT NULL,  -- Unix timestamp
                created_at TEXT NOT NULL DEFAULT (datetime('now', 'localtime'))
            )
        """)
        
        # provider_results 表 (DB-03)
        conn.execute("""
            CREATE TABLE IF NOT EXISTS provider_results (
                id INTEGER PRIMARY KEY AUTOINCREMENT,
                session_id TEXT NOT NULL,
                provider TEXT NOT NULL,
                response_time_ms INTEGER NOT NULL,
                results_count INTEGER NOT NULL DEFAULT 0,
                error_type TEXT,  -- 可为空，成功时无错误类型
                error_message TEXT,  -- 可为空
                status TEXT NOT NULL,  -- 'success' 或 'error'
                created_at TEXT NOT NULL DEFAULT (datetime('now', 'localtime')),
                FOREIGN KEY (session_id) REFERENCES comparison_sessions(session_id)
            )
        """)
        
        # search_results 表 (DB-04)
        conn.execute("""
            CREATE TABLE IF NOT EXISTS search_results (
                id INTEGER PRIMARY KEY AUTOINCREMENT,
                session_id TEXT NOT NULL,
                provider TEXT NOT NULL,
                rank INTEGER NOT NULL,  -- 排序位置
                title TEXT NOT NULL,
                url TEXT NOT NULL,
                description TEXT,  -- 可为空
                published_date TEXT,  -- 可为空，ISO 8601 格式
                source_domain TEXT NOT NULL,  -- 从 URL 解析的域名
                created_at TEXT NOT NULL DEFAULT (datetime('now', 'localtime')),
                FOREIGN KEY (session_id) REFERENCES comparison_sessions(session_id)
            )
        """)
        
        conn.commit()
        self._logger.debug("Tables created: comparison_sessions, provider_results, search_results")
    
    def _create_indexes(self, conn: sqlite3.Connection) -> None:
        """创建索引
        
        索引设计:
        - comparison_sessions.timestamp: 时间范围查询
        - provider_results.session_id, provider: 会话和供应商筛选
        - search_results.session_id, provider, source_domain: 多维度查询
        """
        # comparison_sessions 索引
        conn.execute("""
            CREATE INDEX IF NOT EXISTS idx_sessions_timestamp 
            ON comparison_sessions(timestamp)
        """)
        
        # provider_results 索引
        conn.execute("""
            CREATE INDEX IF NOT EXISTS idx_provider_results_session_id 
            ON provider_results(session_id)
        """)
        conn.execute("""
            CREATE INDEX IF NOT EXISTS idx_provider_results_provider 
            ON provider_results(provider)
        """)
        
        # search_results 索引
        conn.execute("""
            CREATE INDEX IF NOT EXISTS idx_search_results_session_id 
            ON search_results(session_id)
        """)
        conn.execute("""
            CREATE INDEX IF NOT EXISTS idx_search_results_provider 
            ON search_results(provider)
        """)
        conn.execute("""
            CREATE INDEX IF NOT EXISTS idx_search_results_source_domain 
            ON search_results(source_domain)
        """)
        
        conn.commit()
        self._logger.debug("Indexes created for all tables")
    
    def table_exists(self, table_name: str) -> bool:
        """检查表是否存在
        
        Args:
            table_name: 表名称
            
        Returns:
            bool: 表是否存在
        """
        conn = self.get_connection()
        try:
            result = conn.execute(
                "SELECT name FROM sqlite_master WHERE type='table' AND name=?",
                (table_name,)
            ).fetchone()
            return result is not None
        finally:
            conn.close()
    
    def get_table_count(self) -> int:
        """获取已创建表数量"""
        conn = self.get_connection()
        try:
            result = conn.execute(
                "SELECT COUNT(*) FROM sqlite_master WHERE type='table'"
            ).fetchone()
            return result[0] if result else 0
        finally:
            conn.close()
```

**遵循 DDD 架构模式:**
- 模块放在 `infrastructure/database/` 目录
- 核心操作记录日志 (初始化、表创建、错误)
- 中文文档字符串
- D-02: 使用 sqlite3 标准库，每次操作新建连接
- D-01: `init_database()` 幂等，使用 `CREATE TABLE IF NOT EXISTS`
- D-05: 表名 snake_case
</action>
<acceptance_criteria>
- `grep "class DatabaseManager" melodyi_search/infrastructure/database/database_manager.py` 返回匹配
- `grep "def init_database" melodyi_search/infrastructure/database/database_manager.py` 返回匹配
- `grep "CREATE TABLE IF NOT EXISTS comparison_sessions" melodyi_search/infrastructure/database/database_manager.py` 返回匹配
- `grep "CREATE TABLE IF NOT EXISTS provider_results" melodyi_search/infrastructure/database/database_manager.py` 返回匹配
- `grep "CREATE TABLE IF NOT EXISTS search_results" melodyi_search/infrastructure/database/database_manager.py` 返回匹配
- `grep "CREATE INDEX IF NOT EXISTS idx_sessions_timestamp" melodyi_search/infrastructure/database/database_manager.py` 返回匹配
- `grep "from melodyi_search.infrastructure.database.database_manager import DatabaseManager" melodyi_search/infrastructure/database/__init__.py` 返回匹配
</acceptance_criteria>
<done>
DatabaseManager 类实现完成，包含 init_database()、get_connection()、_create_tables()、_create_indexes() 方法。__init__.py 导出 DatabaseManager。
</done>
</task>

</tasks>

<verification>
**手动验证步骤:**

1. 验证模块导入:
```bash
cd C:/workspace/melodyi-skills/skills/melodyi-search
python -c "from melodyi_search.infrastructure.database import DatabaseManager; print('Import OK')"
```

2. 验证数据库初始化:
```bash
python -c "
from melodyi_search.infrastructure.config.config_loader import load_config
from melodyi_search.infrastructure.database import DatabaseManager
config = load_config()
db = DatabaseManager(config.database)
db.init_database()
print(f'Tables created: {db.get_table_count()}')
print(f'comparison_sessions exists: {db.table_exists(\"comparison_sessions\")}')
print(f'provider_results exists: {db.table_exists(\"provider_results\")}')
print(f'search_results exists: {db.table_exists(\"search_results\")}')
"
```

3. 验证数据库文件创建:
```bash
ls ./data/compare.db
```

4. 验证幂等性 (重复执行不会报错):
```bash
python -c "
from melodyi_search.infrastructure.config.config_loader import load_config
from melodyi_search.infrastructure.database import DatabaseManager
config = load_config()
db = DatabaseManager(config.database)
db.init_database()
db.init_database()  # 重复执行
print('Idempotent OK')
"
```

5. 验证配置加载:
```bash
python -c "
from melodyi_search.infrastructure.config.config_loader import load_config
config = load_config()
print(f'Database path: {config.database.database_path}')
"
```
</verification>

<success_criteria>
1. DatabaseManager 模块可正常导入
2. 数据库文件在 `./data/compare.db` 创建成功
3. 三张表 (comparison_sessions, provider_results, search_results) 创建成功
4. 六个索引创建成功
5. init_database() 幂等操作，重复执行不报错
6. 配置 model 包含 DatabaseConfig，默认路径为 `./data/compare.db`
</success_criteria>

<output>
完成后创建 `.planning/phases/01-database-infrastructure/01-01-SUMMARY.md`
</output>